import { StyleSheet } from 'react-native';
const EditProfileStyleSheet = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'flex-start',
        height: 100
    },

    marginForm: {
        width: 300,
        marginTop: 50
    },

    viewButton:{
        marginTop: 20,
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-around',
    },

});

export default EditProfileStyleSheet;